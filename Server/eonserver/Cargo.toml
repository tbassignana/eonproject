[package]
name = "eonserver"
version = "0.1.0"
edition = "2021"
description = "SpaceTimeDB game server module for Eon Project - 3rd person multiplayer game"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = "1.0"
log = "0.4"

[profile.release]
opt-level = "s"
lto = true
